<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pielikes.appurfuone">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcher_logo_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/launcher_logo_1"
        android:supportsRtl="true"
        android:theme="@style/UrfuTheme">
        <activity
            android:name=".ChoseActivity"
            android:label="@string/app_name"
            android:theme="@style/UrfuTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/UrfuTheme.NoActionBar" />

        <provider
            android:name=".CProvider"
            android:authorities="ru.pielikes.database"
            android:exported="true" />

        <activity android:name=".InstituteEIM" />
        <activity android:name=".DirectionBI" />
        <activity android:name=".DirectionMEVO"></activity>
    </application>

</manifest>